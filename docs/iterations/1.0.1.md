# Iteration 1.0.1 — Changed

## Changes
- .agents protocol reference | 2026-07 layout → + speakmcp-settings.json, layouts/, .backups/ | protocol additions since the snapshot
- Memory entry format | id/title/importance/tags → + content field, non-YAML frontmatter note | protocol now documents content and the simple key/value format
- .agents Hub | absent → mentioned | protocol added a sharing catalog for bundle artifacts
- spec-snapshot | 2026-07 → 2026-08 | re-verified against live specs (agents.md, dotagentsprotocol.com, agentskills.io/specification)
- Version | 1.0.0 → 1.0.1 | patch bump for the reference update

## Implementation notes
- Re-checked all three specs against the live sources: agents.md and the Agent
  Skills spec are unchanged since the snapshot; only the .agents protocol had
  additions (layout entries, memory `content` field, .agents Hub).
- agents-md.md, skill-creator/references/spec.md, and distribution.md needed no changes.

## TODO / rollback notes
- Push to main triggers the release action: tag + GitHub Release v1.0.1.
- Rollback: revert commit, delete tag (`git tag -d v1.0.1`).